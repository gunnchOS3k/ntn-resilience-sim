# Legacy — placeholder README

Prior `docs/uml/README.md` only said to see SpectrumX. That is not the architecture of this simulator. Current diagrams are generated from `src/ntn_resilience` and `src/sites`.
